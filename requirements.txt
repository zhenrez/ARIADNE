# ARIADNE currently uses only the Python standard library.
# Keep this file so START-ARIADNE.cmd has one stable dependency-install step.
# Future third-party dependencies must be pinned here before the launcher uses them.
